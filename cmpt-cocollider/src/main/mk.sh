#!/bin/bash
set -u
set -e

LOCAL_PATH=`pwd`
OUT_PATH=$1
BUILD_TYPE=$2

# ------------------------------------------------------------------------------------------------------
ndk-build -C jni clean
if [ $BUILD_TYPE = 'debug' ] ; then
  ndk-build -C jni NDK_DEBUG=1
else
  ndk-build -C jni
fi

# ------------------------------------------------------------------------------------------------------
# ------------------------------------------------------------------------------------------------------
#rm -rf ${OUT_PATH}/libs
#mv libs ${OUT_PATH}

# ------------------------------------------------------------------------------------------------------
rm -rf obj
#rm -rf libs
